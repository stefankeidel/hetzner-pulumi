name: hetzner-pulumi
description: A minimal Python Pulumi program
runtime:
  name: python
  options:
    toolchain: uv
    virtualenv: ".venv"
config:
  pulumi:tags:
    value:
      pulumi:template: python
